import React from 'react'
 
function SpecialText({ title, subTitle }) {
    return (
        <div className='about__title'>
            <p className=''>{title}</p>
            <p className='sub__name'>{subTitle}</p>
            
        </div>
    )
}

export default SpecialText
